# PostgreSQL (local, docker-compose)
DATABASE_URL="postgresql://marketing:marketing@localhost:5433/marketing_ai?schema=public"

# Claude Agent SDK auth (géré par le SDK — voir CLAUDE.md)
# CLAUDE_CODE_OAUTH_TOKEN=...
